# Database Configuration
# You'll need to replace this with your actual database URL
# For local development, you can use a local PostgreSQL instance or a cloud database like Neon
DATABASE_URL="postgresql://neondb_orequire"

# Server Configuration
PORT=5000
NODE_ENV=development

# Session Configuration
# Generate a secure random string for session secret
SESSION_SECRET="neondb-scientific-waste-map-session-secret-2025"

# Replit Configuration (if using Replit)
# These are only needed if you're running on Replit platform
# REPLIT_DOMAINS="your-replit-domain.replit.dev"
# REPL_ID="your-repl-id"
# ISSUER_URL="https://replit.com/oidc"
